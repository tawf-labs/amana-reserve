[package]
name = "amana-private"
version = "0.1.0"
description = "AMANA Private operations with TEE support"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "amana_private"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.1"
anchor-spl = "0.30.1"
ephemeral-rollups-sdk = { version = "0.1", features = ["anchor"] }
